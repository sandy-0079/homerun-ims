# Handoff — 2026-07-30 (end of day) · **refreshed 2026-07-31 morning**

Supersedes `HANDOFF-2026-07-29.md` (deleted). **Delete this file and the pointer at the top of
`CLAUDE.md` once Stages 5–6 land.** `CLAUDE.md` holds durable knowledge; this holds only in-flight state.

> **Refreshed 2026-07-31** after the morning checks: §START HERE now carries their results and points at
> tonight, and the three facts that had gone stale are corrected (git `5eee384` → **`3411b55`**, frontend
> `index-PiLWVd0V.js` → **`index-DIVCFU9e.js`**, tests 333 → **340**, shadow 4,078 → **5,307** rows).
> Everything below §3 is still the 07-30 snapshot unless marked otherwise.

⚠ **Every number here is a SNAPSHOT.** Sandy uploads CSVs between sessions and four crons write
overnight. §4 has commands to re-derive live state — do that before trusting anything below.

**Four independent deployment surfaces still apply** — Vercel frontend (`git push`), **edge functions
(`supabase functions deploy`, NOT git)**, Supabase data rows, **pg_cron (a migration, NOT committing the
.sql)**. "Is it live?" always needs asking per surface.

---

## ▶ START HERE (next session, morning of 2026-08-01)

> **Prompt to open the session with:**
> `Read docs/HANDOFF-2026-07-30.md and start with the morning checks.`

**Agreed order:** ~~morning checks~~ ✅ → **SKU-floor sheet sync** ← *you are here* → Stage 6. Stage 5
stays on hold, accruing clean nights.

⚠ **Before writing any code, the one decision still to settle is the SKU-floor sheet's row-collapse
guard** — §5 item 2 has the argument. Everything else about that sync is agreed and its recon is clean,
so that decision is the only thing standing between reading and building.

**Two things to check tonight/tomorrow morning, both one curl (§4):**

1. **`params/catalogueSyncStatus`** — the **normal** path, not yet exercised. `lastOkNight` should
   become **`"2026-07-31"`** (a slot actually doing the pull). If it is still `"2026-07-30"`, no slot
   succeeded — check `ok`/`reason` and the logs.
2. **`params/invoiceSyncStatus`** — **night 3** of the Stage 5 count. Expect `phase: published`,
   `degradedDates: []`, plan **`[2026-07-31, 2026-07-28]`**.
   - **Falsifiable prediction: 07-28 heals.** It has been flagging at 25.5% because its rows predate
     the status-blocklist fix and nothing had re-fetched them. Confirmed from code —
     `planNightDates` returns `[yesterday, yesterday − 3]` — that tonight is 07-28's D-3 re-fetch, and
     `mergeInvoiceRows` replaces a fetched date wholesale. If `compare-invoice-shadow.mjs` still shows
     07-28 at ~25% tomorrow, **something is wrong with the re-fetch**, not with the old data.

### ✅ Morning checks of 2026-07-31 — both PASSED

**`catalogueSyncStatus`: the gate is proven.** `lastOkNight: "2026-07-30"` unchanged, `at` still
`09:06:53Z` (the 14:36 IST manual run). Logs confirm **all five slots fired and all five skipped** —
16:25, 16:55, 17:25, 17:55, 18:25 UTC, each logging `already ran tonight (2026-07-30), nothing to do`,
**zero Zoho calls**. So **five slots ≠ five pulls** is now demonstrated, not hoped for.
- ⚠ Note how it had to be proven: the early return at `sync-catalogue/index.ts:122` deliberately does
  **not** call `setStatus` (stamping `at` would corrupt the 15-min cooldown reference), so on a skip the
  status row is byte-unchanged. **The row alone cannot distinguish "five slots skipped" from "the cron
  never fired"** — the exact ambiguity that hid 07-29's failure. Resolved from logs; see the
  `function_edge_logs` technique now in `CLAUDE.md`.

**`invoiceSyncStatus`: night 2 clean.** `phase: published` · plan `[07-30, 07-27]` · `degradedDates: []`
· coverage **0% unknown** (0 distinct) · `pinPct: 100` · merge `safe`, `datesReplaced: 1` · rows
4,078 → **5,307**, dates 4 → 5 · cursor fully drained. Published 21:20:07Z in 6s; the 22:05 and 22:20
slots correctly returned "already published". **Zero 429s** — verified with a *filtered* query, because
the first sampled one hit the 1000-row cap (see the ⚠ now in `CLAUDE.md`). `statusSeen` = paid 1168 /
void 13 / overdue 9, no `partially_paid`/`sent`, as expected on settled days.

**The D-3 re-fetch demonstrably works.** 07-27 healed **0.6% → 0.1%** (1254 vs 1253, +2 qty, 1 SKU×DS)
— and the drift direction is **over**, i.e. freshness, not a leak. 07-26 unchanged at 0.2% (the known
invoice voided between the CSV export and the re-fetch — **the shadow is the more correct side**).

**Stage 5 clean-day count: 2 of ~5.**

### ⚠ A ~50-minute org-wide Zoho 429 window, 2026-07-30 15:41–16:30 UTC

Killed `stock-sync-3` (DS04+DS05), `stock-sync-4` (DS06), `orders-sync`, and a browser TO-tool pull.
All recovered 16:41–16:53; each branch pair missed exactly one cycle. **Ops absorbed it** — the TO team
saw the staleness, repulled, raised TOs, ops resumed.

**Full account, evidence and the decisions taken are now in `CLAUDE.md`** (the "ZOHO GOES DOWN
ORG-WIDE — TWICE NOW" entry, plus two additions to the edge-logs section). Headlines:
- **Not our pacing.** Two *different* rate-limit buckets failed inside ten minutes
  (`inventorysummary` at 15:41/15:44, `/purchaseorders`+`/transferorders` at 15:50). Four
  self-inflicted hypotheses ruled out with measurements. It died on the **first** call → someone
  else's penalty. Second occurrence in two days; **a third makes it a pattern.**
- **`:35`/`:38` did NOT fail** — a browser-held session lease (12-min TTL) blocked them silently, and
  `:41`/`:44` ran because it had expired. That split is the signature.
- **Deliberately NOT fixed (2026-07-31).** Instrumenting the 429 (headers + body in
  `_shared/zohoClient.ts`) was considered and **rejected**: shared path for all five functions, the
  body-consumption hazard lands exactly on the `retry429:false` branch that only `create-to` uses, and
  the payoff is diagnostic-only on an event ops absorbs with a repull. Same for the skip-path logs and
  the token single-flight. **Rule adopted: piggyback observability on a deploy you are already making
  for a substantive reason; never deploy solely for observability.**

---

## 1. What today established

**Stage 4 night 1 PASSED on mechanism.** Zero 429s across 1,000 log lines, 19:00–22:40 UTC. Chunking
across invocations, the atomic publish, `endOffsetDays` and CONCURRENCY-4 pacing all worked first time:

```
19:05, 19:20  07-29 chunks
20:05         07-29 complete — 1 date left
20:20         07-26 listed 336 · chunk done · 0 to retry
21:05         PUBLISHED 07-29, 07-26 → invoice_data_shadow — 4078 rows, 0% unknown, 7s
21:20, 22:05, 22:20   "plan already published tonight, nothing to do"   ← zero Zoho calls
```

**Reconciliation: 9 differences, every one root-caused, none a leak.** vs a fresh 07-29 CSV export
(taken 11:33 IST; the pull ran 02:35 IST — nine hours of Zoho drift):

| Invoice | What changed after the pull | Effect |
|---|---|---|
| `HC/26/014517` | **Voided** (CSV shows all 6 lines `Void`) | shadow over-counts 5 rows / 5 qty, DS03 |
| `HC/26/014555` | Created/confirmed after the pull | shadow under 2 rows / 3 qty, DS06 |
| `HC/26/014415` | Line item `BQV7U` added after the pull | shadow under 1 row / 1 qty, DS04 |

Arithmetic closes exactly: `+5 −2 −1 = +2` rows (1288 vs 1286), `+5 −3 −1 = +1` qty (7928 vs 7927).
07-26 (the D-3 re-fetch, settled) had **one** difference — an invoice in the CSV but voided before the
re-fetch, i.e. **the shadow was the more correct of the two.** The generalisable lesson (now in
`CLAUDE.md`): **a leak subtracts only; a freshness gap goes both ways.**

**`sync-catalogue`'s first real run failed and left no trace.** 18:25 UTC, HTTP 500,
`Zoho API: 429 after 3 attempts`, dead on `/items` page 1 — and `params/catalogueSyncStatus` did not
exist, so it was indistinguishable from "the cron never fired". Every Zoho consumer failed in the same
17:35–18:30 UTC window and all recovered at 18:35. Not recurring, not our load, not the token cache.
Both defects fixed and shipped; see `CLAUDE.md` Stage 7 and the sync-constraints section.

---

## 2. Exactly what is live now

**git:** `main` == `origin/main` == **`3411b55`**, clean (verified 2026-07-31 morning). The 07-30
session's own snapshot said `5eee384`; six commits landed after it, **one of them code** — `1a063cb`
(the invoice ⬇ Data round-trip fix) — the rest docs (`b4c9fa9`, `0c85845`, `f9cb858`, `c9c798a`,
`3411b55`). Earlier that day: `87b9593` (sync-catalogue hardening), `96a1bf4` (the `saveTeamData` fix),
`b68319b` (docs), **`b425047` (engine: active-only targets)**, `32ec127` + `5eee384` (Upload Data tab).

**⚠ ENGINE BEHAVIOUR CHANGED TODAY — `b425047`, live for every user on next page load.**
Only SKUs `active` in the SKU Master now get non-zero targets. See the "Active-only normalization"
section in `CLAUDE.md` for the full account. What matters here:
- **9 SKUs** lost targets — 4 in the master but not active, 5 absent from it (pre-July orphan codes).
  `Z8DJK` losing 10 units ≈ ₹1.6L was confirmed expected by Sandy.
- **Inv Value Max ₹8.0195Cr → ₹7.9952Cr**, Min **₹5.6369Cr → ₹5.6238Cr** — the Overview card reads
  **8.02 → 8.00**, −₹2.43L. ⚠ The `b425047` commit message states this as "₹8.00Cr → ₹7.98Cr", which is
  **wrong** — that mislabels the active-only subtotal as the *before* figure. The commit was already
  pushed and deployed, so its message was left alone rather than rewriting three deployed SHAs; these
  figures and `CLAUDE.md` are authoritative.
- **`toTargets` is byte-identical** rebuilt from the new engine (0 SKUs lost/gained/changed), so the TO
  tool and the 14:30/20:30 runs were never affected. Verified before pushing.

**Upload Data tab reworked** (`32ec127`, `5eee384`): canonical counts from `src/inputSummary.js` shared
with Overview, AUTO/MANUAL buckets, per-input provenance from `params/uploadProvenance` (a NEW row,
browser-only writer), and a `Last run:` stamp beside the Apply button. Three numbers were wrong before:
Invoice `74,381 rows → 35,533 invoices`, SKU Master `2,105 → 2,101 active`, and New DS Floor Qty
`1,921 → 1,021` (900 stored entries are `0`, and a zero floor is the absence of a floor).

**Edge functions — still exactly FIVE.** `sync-catalogue` redeployed from `87b9593`; the
one-commit-behind drift noted in the previous handoff is resolved. `sync-stock`, `sync-orders`,
`create-to`, `sync-invoices` untouched today.

**Frontend: `index-DIVCFU9e.js`** — re-verified 2026-07-31: served by `homerun-ims.vercel.app` and
matching a local build of **`3411b55`**. So the invoice ⬇ Data round-trip fix (`1a063cb`) **is live**,
along with the `teamDataBundle` fix, the active-only engine rule and the reworked Upload Data tab.
(The 07-30 snapshot recorded `index-PiLWVd0V.js` / `5eee384` — superseded, Vercel rebuilt on the later
pushes.)

**Crons — 8 jobs, no two sharing a minute** (verified from `cron.job`):

```
:35 stock-sync-1  :38 stock-sync-2  :41 stock-sync-3  :44 stock-sync-4  :50 orders-sync-hourly
25,55 16,17 * * *   catalogue-sync-earlier   ← NEW (20260730000001, applied)   21:55–23:25 IST
25 18 * * *         catalogue-sync-nightly         (20260729000002, untouched)      23:55 IST
5,20 19-22 * * *    invoices-sync-window                                       00:35–04:00 IST
```

Migration ledger clean, local == remote through `20260730000001`.

**`TARGET_ROW = "invoice_data_shadow"` — Stage 5 is still one constant.** Nothing reads that row.

**Tests: 340 passing over 31 files** (re-run 2026-07-31; was 257 on the morning of 07-30). New on 07-30:
`_shared/syncCooldown.test.js` (+11, the night gate), `src/teamDataBundle.test.js` (12),
`src/inputSummary.test.js` (18), `src/freshness.test.js` (25),
`src/engine/__tests__/activeOnly.test.js` (10) and `invoiceCsvRoundTrip.test.js` (7, the +7 over the
333 that snapshot recorded). Lint unchanged at 68 errors.

**Live data state (verified after Sandy's floors upload + Apply):**

| Row | State |
|---|---|
| `team_data/global` | skuMaster **2,105** (active 2,101) · priceData **1,858** · invAt DC 2,034 / Supplier 58 / DS 13 |
| | newSKUQty **1,148** · minReqQty **1,921** · deadStock **20** |
| `team_data/invoice_data_shadow` | **5,307 rows, 5 dates (2026-07-26 → 07-30)** after night 2 |
| `params/toTargets` | **2,030** SKUs · `refreshedAt` 2026-07-30T12:52:58Z (18:22 IST) |
| `params/catalogueSyncStatus` | `ok:true` · `lastOkNight: 2026-07-30` |
| `params/uploadProvenance` | **NEW row, now populated** — `invoiceData`/`newSKUQty`/`minReqQty`/`deadStock` all stamped 12:51–12:52Z. `skuMaster`/`priceData` deliberately absent: they are auto-synced, so their timestamp comes from `catalogueSyncStatus.at` |
| `params/global` | 11 category strategies (incl. `Kitchen Sinks & Faucets` → PCT) · attribution `shippingCode`, 128 pincodes |

`team_data/invoice_data` was **re-uploaded at 18:21 IST** from
`~/Downloads/IMS Uploads 29 July/Invoices - Apr30_Jul28.csv` — **content-identical** (74,381 rows,
90 dates, qty 471,746, 6,231 SKU×DS combos, 0 differing), done purely to stamp provenance. The file was
validated against the stored row BEFORE upload; see the coverage ⚠ in `CLAUDE.md`.

**The `saveTeamData` fix got its real test.** Four uploads inside 90 seconds, and `skuMaster` stayed
**2,105** with `priceData` at **1,858** — the values the 14:36 catalogue sync wrote. Under the old code
every one of those four writes would have rewritten both from the tab's React state. The morning test
passed only because the tab was fresh; this one passed on the code.

Also live: `SMBTV` floor changed to 2/5 on DS01–DS05, which the DS Seed propagated to DS06 as well
(2/2 → 2/5) — see the six-store ⚠ under DS Seed in `CLAUDE.md`. ~+14 units, ~+₹8.7K.

---

## 3. Verified today, worth not re-deriving

- **The `saveTeamData` clobber is closed and prod-tested.** A floors upload moved `publishedAt`
  09:26:23 → 10:02:40 while `skuMaster` stayed 2,105 and `priceData` 1,858. ⚠ Honest caveat: that tab
  was a fresh Incognito load, so **the old code would have passed the same test** — nothing was stale to
  write back. The bug itself is proven by the unit test that reproduces it directly. The live
  stale-tab demonstration (leave a tab open → force a catalogue sync → upload from that tab) was
  offered and not run; two minutes if anyone wants it.
- **A reload→Apply round trip is lossless.** Fresh Incognito load + Apply → all 7 `params` rows
  byte-identical bar `paramsBackup._backedUpAt` and `toTargets.refreshedAt`. This closes Sandy's
  standing worry that "a hard reload changed a lot of params in the Logic Tweaker" — the historic cause
  was the `loadParamConfigRows` bug, now fixed, and the write is always an Apply, never the reload.
- **`toTargets.targets` was byte-identical across two Applies with the same inputs** — a free
  determinism check.
- **`applyAndRun` writes only the `params` table.** It never calls `saveTeamData`, so Apply cannot touch
  `team_data/global`.

---

## 4. Commands

```bash
B=https://rgyupnrogkbugsadwlye.supabase.co/rest/v1; K=<anon key from CLAUDE.md>

# The night's results
curl -sS "$B/params?select=payload&id=eq.catalogueSyncStatus" -H "apikey: $K" | python3 -m json.tool
curl -sS "$B/params?select=payload&id=eq.invoiceSyncStatus"   -H "apikey: $K" | python3 -m json.tool
curl -sS "$B/params?select=payload&id=eq.invoiceSyncCursor"   -H "apikey: $K"   # must be drained

# Shadow vs the live CSV-uploaded row (⚠ cumulative — see the CLAUDE.md caveat)
node scripts/compare-invoice-shadow.mjs

# Shadow vs a fresh CSV export. ⚠ Zoho still exports DD/MM/YYYY, so parseInvoiceCsv will THROW.
node scripts/compare-csv-vs-shadow.mjs ~/Downloads/Invoice.csv
```

**Edge function logs** (no dashboard needed): Management API
`GET /v1/projects/{ref}/analytics/endpoints/logs.all?sql=…&iso_timestamp_start=…` over
`function_logs` / `function_edge_logs`. Token: `security find-generic-password -s "Supabase CLI" -w`,
strip the `go-keyring-base64:` prefix and base64-decode. **Send a browser `User-Agent`** or Cloudflare
answers `403 code 1010`. Cap at 1000 rows; split by time window to attribute logs per invocation.

⚠ **Do not loop verification against Zoho.** One run, then wait. `force: true` bypasses the 15-min
cooldown — don't.

---

## 5. Still open

**The dependency chain, agreed 2026-07-30 evening.** ⚠ Note the correction: the TO tool ALREADY picks up
the latest Min/Max — it reads `params/toTargets` on load. The gap is not the tool, it is that **nothing
writes that row except a human clicking Apply** (`App.jsx:3312` is still the only writer). So:

```
recon clean  (invoices · catalogue · SKU floors)
      ↓
Stage 5   flip TARGET_ROW → the invoice sync feeds the LIVE row
      ↓
Stage 6   headless engine auto-runs → WRITES toTargets        ← the actual blocker
      ↓
TO tool has the latest Min/Max with no Apply needed
      +   surface toTargets.refreshedAt beside its existing stock freshness
          (small, display only — refreshedAt already exists and is written on every run)
```


1. **Freshness timestamp — NEXT, and there are now FOUR arguments for it.** A 24h-stale catalogue that
   nobody could see; a stale-tab compute path that is still open; and two added by the 07-30 429 window:
   **ops discovered the stock staleness themselves** rather than being told, and **a browser-held sync
   lease can leave branches stale for a full hour with no signal at all** if the tab goes away after
   `sessionStart` (see the lease ⚠ in `CLAUDE.md`). That last one is explicitly the reason to surface
   freshness rather than shorten the lease TTL, which exists to prevent the 2026-07-09 429 storm.
   It is also frontend-only — no cron, no edge function, revertible by a revert — which makes it the
   lowest-risk item on this list, not just the cheapest. Cheapest first step unchanged:
   `toTargets.refreshedAt` **already exists** and is written on every Apply — it is simply displayed
   nowhere. Surface that plus `catalogueSyncStatus.at`, enrich with `invoiceDataThrough` and
   `engineCommit` when Stage 6 lands.
   - ⚠ `invoiceDataThrough` matters more than `refreshedAt`: a run timestamp says a computer did
     something, not that the answer is current. A fresh-looking stamp over stale inputs is worse than none.
   - Placement: IMS header pill (`styles.header`, `App.jsx:80`, `marginLeft:auto` so it shows on every
     tab); fuller breakdown on Upload Data. TO tool: beside the existing stock freshness, reusing
     `isFresh()` from `homerun-to/src/sync.js`.
2. **SKU-floor sync from a Google Sheet — UNBLOCKED, RECON CLEAN, PICK UP 2026-07-31.**
   - **Sheet** (published to web, readable by anyone with the URL — accepted):
     `https://docs.google.com/spreadsheets/d/e/2PACX-1vTT2_CBSySgwSk_DVQEziLMzrTxWxmuVDZ1npn6qb5jIeN2zBbNAQWPRZf-r3A7tb_mreZtAgNSJYFh/pubhtml?gid=0&single=true`
     ⚠ That is the `pubhtml` URL. The CSV export is a **different path** — swap `pubhtml` for
     `pub` and add `&output=csv`:
     `.../pub?gid=0&single=true&output=csv`
   - **Layout:** `SKU, DS01 Min, DS01 Max, … DS06 Min, DS06 Max`. Note it includes **DS06**, unlike the
     CSV template which stops at DS05. A new store (DS07) will be **appended as columns to the right**.
   - **RECON, 2026-07-30 (read-only, no code):** HTTP 200 `text/csv`, header an exact match, 1,148 rows /
     1,148 distinct SKUs, **0 duplicates, 0 invalid values**, and vs live `newSKUQty`: **0 added,
     0 removed, 0 changed**, 0.0% row collapse. The sheet is a faithful mirror of prod — so the first
     real run is a provable no-op, which is the right way to launch a replace-entirely writer.
   - **⚠ THE MOST VALUABLE OUTPUT HAS NOTHING TO DO WITH SYNCING: 39 of 1,148 floors (3.4%) can never
     take effect,** and nothing tells ops today. 38 SKUs are absent from `skuMaster` — all pre-July
     orphan codes (`FAC-EBCO-*`, `TIL-MYK-*`, `POWB4-40-AT`) — and `Z8DJK` is not Active so it is zeroed.
     Ops maintains 1,148 believing all are live; **1,109 are.** Same orphan population that shows up in
     the invoice unknown-SKU rate and drove the active-only engine fix — the ~2026-07-01 Zoho re-code
     left a trail across three inputs and each surface only ever showed its own slice.
   - **Three points agreed for tomorrow:**
     1. **Settle the row-collapse guard first.** The earlier decision was sheet-authoritative with
        "reporting, not blocking". Today argues against it: the invoice guard *reports* format and still
        cannot stop a short file — what actually protected the upload was comparing **coverage** before
        writing. A filter left applied, or a revoked publish serving Google's HTML error page, would
        silently drop most floors, and `1,148 → 3` passes a report-only design. Keep the sheet
        authoritative, but **fail closed** on a collapse beyond a threshold, same discipline as
        `assessCoverage` / `mergeInvoiceRows`.
     2. **Build with the dry-run path first, deploy DORMANT (no cron)** — the proven house pattern
        (Stage 4 shipped shadow-only, Stage 7 shipped dormant).
     3. **Schedule at ~04:00 IST** — after `invoices-sync-window` closes, and after `sync-catalogue`,
        so a SKU created in Zoho overnight already exists in `skuMaster` and its floor is not skipped
        for no reason.
   - **⚠ Parse columns BY HEADER NAME, not position** (DS07 will be appended). And adding DS07 columns
     alone is not enough — `DS_LIST` in the app must gain DS07 too, or the sync should flag it as an
     unknown DS and skip it. Fail loudly; never write floors for a store the engine does not know.
   - **⚠ DS06 columns now interact with the DS Seed.** All DS06 cells are currently `0,0`, so nothing is
     set and the seed still fills DS06 — same as today. Putting a real value there would compete with
     the seed, and since every post-blend step is a per-field `max`, **whichever is higher wins**
     (`dsSeed.js:36-37`) — the seed does not automatically override a floor.
   - Remaining decisions already settled and unchanged: `0` removes a floor, integers ≥ 0 only, unknown
     SKUs **skipped and named**, DS validated against `DS_LIST`, duplicate `SKU×DS` rejected, and the
     **header row checked** before trusting any parse.

3. ~~**SKU-floor sync from a Google Sheet — BLOCKED ON SANDY**~~ (superseded by item 2 above) for three things: the **sheet URL**, the
   **column layout** (`SKU×DS` matrix vs `SKU,DS,Qty` rows), and **whether it is published to web**
   (publishing makes it readable by anyone with the URL). All the design decisions are settled and
   recorded in the previous handoff's §5b — sheet is authoritative, absent SKU ⇒ floor dropped,
   integers ≥ 0 with `0` removing, unknown SKUs skipped **and named**, validate DS against `DS_LIST`,
   duplicate `SKU×DS` rejected, and **check the header row** because a revoked publish makes Google
   serve an HTML error page that `parseCSV` turns into garbage rows.
3. **Stage 5** — flip `TARGET_ROW`. Night 1 clean; ~4 more to go. **Re-verify the backup immediately
   before the flip** — it goes stale the moment anyone uploads a CSV, and nothing before 2026-07-01 is
   re-fetchable, so a stale backup is worse than none.
4. **Stage 6** — headless engine → `params/toTargets`, as a **Vercel Cron in this repo** (not a Supabase
   edge function): `src/engine/` has zero browser dependencies, so a Vercel function can import it
   directly, which *eliminates* the two-implementations drift rather than detecting it. Write to
   `toTargets_shadow` first and diff against a browser Apply.
5. **The stale-tab compute gap** (§3) — needs change-detection or a "catalogue updated, reload" prompt.
   Naively adding `skuMaster` to the realtime handler causes ~5 engine re-runs an hour; the reasoning is
   recorded at the handler itself. Belongs with item 1.
6. **125 active SKUs sit in categories never assigned a strategy** — Home Appliances 57, Glass Hardware
   31, Service 2 (Kitchen Sinks & Faucets 35 was assigned to PCT today). Premium slow-movers on
   Standard averaging. Its own diff, not urgent.
7. ~~Fix the invoice `⬇ Data` round-trip~~ — **DONE 2026-07-30.** `buildInvoiceCsv` now lives beside
   `parseInvoiceCsv` in `engine/utils.js` and the round trip is verified lossless against live data
   (74,381 → 74,381, 0 SKU×DS differing). Was a total-wipe trap: 74,381 → 0.
8. **Optional:** Excess Qty (`SoH − Max`) column on the Stock Health CSV for reverse TOs.

---

## 6. Blocked on you / ops

- **Set `cf_inventorised_at` in Zoho for 12 new SKUs.** They are `active` and currently getting Min/Max
  on a *defaulted* DC classification, and **all 12 also have no price** — which makes PCT use the 95th
  percentile, the most aggressive setting. The price self-heals as purchases land in this org.
  - Kitchen Sinks & Faucets (8): `3VR4E`, `ENZEK`, `BSUT4` (Carysil) · `QB8FM`, `V2ZTV`, `ECS2S`,
    `3LKGQ`, `6MX9B` (Nirali)
  - Tiling / Roff (4): `TA88S`, `G7XZQ`, `AAXB8`, `C5D4A`
  - **CSV prepared 2026-07-30 for bulk upload:** `~/Downloads/cf_inventorised_at - 12 new SKUs.csv`
    (`SKU, Item Name, Inventorised At` — all DC). Authoritative source is
    `params/catalogueSyncStatus` → `master.newSkusDefaulted`; re-derive from there rather than reusing
    this list, since it changes every time ops creates a SKU without the field.
  - ⚠ The item names are full of inch marks (`24" x 18" x 8"`), escaped RFC-4180-style as `""`. If they
    come back mangled after the Zoho import, that is the cause — and note **our own `parseCSV` strips
    embedded quotes** (see the ⚠ in `CLAUDE.md`), so a round trip through IMS would lose them too.
- ~~Fix the Zoho invoice export locale~~ — **dropped 2026-07-30 (Sandy's call).** He will export ISO
  manually and be mindful. ⚠ The consequence stands: a *fresh* export defaults to `DD/MM/YYYY`, which
  the date guard correctly refuses, so the manual-CSV fallback needs a deliberately-formatted export.
- ~~Add pincode 560111 → DS03~~ — **DONE.** Verified live 2026-07-30: `560111 → DS03`, 128 pincodes
  mapped, mode `shippingCode`.
- Confirm ops routing follows the pincode mapping — DS02 is stocked **₹16.4L lighter**.
- Watch DS02 and DS05 in Stock Health for a week.

---

## 7. Rollback

| If | Then |
|---|---|
| Catalogue cron misbehaves | `select cron.unschedule('catalogue-sync-earlier');` — reverts to exactly the pre-07-30 single 23:55 IST fire, because `catalogue-sync-nightly` was never modified |
| A bad catalogue write | Restore `team_data/catalogue_backup_20260729` (skuMaster + priceData) via read-merge-write |
| Invoice sync misbehaves | `select cron.unschedule('invoices-sync-window');` |
| The `saveTeamData` fix misbehaves | Revert `96a1bf4` and redeploy the frontend. Failure mode is loud and reversible — "an upload didn't save" — not destructive |
| Stage 5 damaged history | Restore the dated `invoice_data` backup. **Nothing before 2026-07-01 is re-fetchable.** |
