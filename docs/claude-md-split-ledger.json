{
  "_readme": "Lines present in the frozen baseline that have been deliberately condensed or retired rather than moved. check-claude-md.mjs refuses any OTHER loss. An entry here is a claim that the lesson survives somewhere — not permission to delete it.",
  "condensed": {
    "### DS Seed — new store bootstrap (`src/engine/dsSeed.js`)": "Retired 2026-09-18 with the DS Seed pass (src/engine/dsSeed.js deleted). The pass was replaced by its inverse — Opening Shortly (src/engine/openingStores.js) — because pincode attribution is retroactive, so a store opening now inherits its own real history instead of borrowing its siblings'. These lines describe the behaviour of code that no longer exists; the surviving generalisable rule is recorded in the Opening Shortly section and in docs/superpowers/specs/2026-09-18-ds07-ds08-golive-design.md.",
    "- **⚠ THE \"PRE-LAUNCH WINDOW\" ARGUMENT IS INVALID, and it is easy to re-derive by mistake.** Attribution": "Retired 2026-09-18 with the DS Seed pass (src/engine/dsSeed.js deleted). The pass was replaced by its inverse — Opening Shortly (src/engine/openingStores.js) — because pincode attribution is retroactive, so a store opening now inherits its own real history instead of borrowing its siblings'. These lines describe the behaviour of code that no longer exists; the surviving generalisable rule is recorded in the Opening Shortly section and in docs/superpowers/specs/2026-09-18-ds07-ds08-golive-design.md.",
    "still guarantees baseline breadth. ⚠ The floor does **not** compensate for the seed — the 431-SKU drop": "Retired 2026-09-18 with the DS Seed pass (src/engine/dsSeed.js deleted). The pass was replaced by its inverse — Opening Shortly (src/engine/openingStores.js) — because pincode attribution is retroactive, so a store opening now inherits its own real history instead of borrowing its siblings'. These lines describe the behaviour of code that no longer exists; the surviving generalisable rule is recorded in the Opening Shortly section and in docs/superpowers/specs/2026-09-18-ds07-ds08-golive-design.md.",
    "**⚠ GENERALISABLE — a bootstrap justified by \"no data\" must be re-tested when the DEFINITION of the data": "Retired 2026-09-18 with the DS Seed pass (src/engine/dsSeed.js deleted). The pass was replaced by its inverse — Opening Shortly (src/engine/openingStores.js) — because pincode attribution is retroactive, so a store opening now inherits its own real history instead of borrowing its siblings'. These lines describe the behaviour of code that no longer exists; the surviving generalisable rule is recorded in the Opening Shortly section and in docs/superpowers/specs/2026-09-18-ds07-ds08-golive-design.md.",
    "- **⚠ WHILE ACTIVE, A 5-COLUMN SKU-FLOOR FILE SET SIX STORES — no longer true since the 07-31 sunset,": "Retired 2026-09-18 with the DS Seed pass (src/engine/dsSeed.js deleted). The pass was replaced by its inverse — Opening Shortly (src/engine/openingStores.js) — because pincode attribution is retroactive, so a store opening now inherits its own real history instead of borrowing its siblings'. These lines describe the behaviour of code that no longer exists; the surviving generalisable rule is recorded in the Opening Shortly section and in docs/superpowers/specs/2026-09-18-ds07-ds08-golive-design.md."
  }
}
