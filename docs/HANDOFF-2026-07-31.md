# Handoff — 2026-07-31 (end of day)

Supersedes `HANDOFF-2026-07-30.md` (deleted). **Delete this file and the pointer at the top of
`CLAUDE.md` once Stage 5 lands** — that is the only remaining piece. `CLAUDE.md` holds durable
knowledge and **was brought fully up to date today**; this holds only in-flight state.

⚠ **Every number here is a SNAPSHOT.** Ten crons write overnight. §3 re-derives live state.

**Five deployment surfaces now** — Vercel frontend (`git push`), **Vercel serverless function**
(`api/run-engine.js`, also `git push` — NEW today), edge functions (`supabase functions deploy`),
Supabase data rows, pg_cron (a migration). "Is it live?" needs asking per surface.

---

## ▶ START HERE — tomorrow morning, 2026-08-01

> `Read docs/HANDOFF-2026-07-31.md and start with the morning checks.`

**Tonight is the first night the WHOLE chain runs unattended.** Four checks, in priority order, each
with the expected value so a wrong number is obvious rather than needing interpretation.

```bash
B=https://rgyupnrogkbugsadwlye.supabase.co/rest/v1; K=<anon key from CLAUDE.md>
for r in engineRunStatus skuFloorSyncStatus invoiceSyncStatus catalogueSyncStatus; do
  echo "== $r"; curl -sS "$B/params?select=payload&id=eq.$r" -H "apikey: $K" | python3 -m json.tool
done
```

**1. `params/engineRunStatus` — the brand-new one, never yet run by cron.**
Expect `ok:true` · `mode:"live"` · `wroteTo:"toTargets"` · `reason:"ok"` · `targets` ≈ 2030.
- ⚠ Also check **`timing.totalMs`** — it was 4.9–5.6s today. `maxDuration` is 60s so there is room,
  but a creep toward 10s+ is worth knowing early.
- ⚠ **If the row is ABSENT entirely**, the cron did not fire or `pg_net` could not reach Vercel. That
  is the one failure mode with no trace, because `pg_net` is fire-and-forget.
- `change.counts` will NOT be 0 — the catalogue sync adds SKUs overnight. `added`/`removed` name them.

**2. `params/skuFloorSyncStatus` — first unattended run.**
Expect `lastOkNight:"2026-08-01"` · `ok:true` · `change.reason:"ok"` · **counts all 0** (the sheet and
prod were made identical today). Still `"2026-07-31"` ⇒ neither slot succeeded.

**3. THE STAGE 5 DECISION NUMBER.** `node scripts/compare-invoice-shadow.mjs`
**Does 07-28 come back at ~1,205 rows instead of 898?** Tonight's plan is `[07-31, 07-28]`, so this is
07-28's **first fetch under the fixed status blocklist** — it directly re-tests the failure that lost
27.7% of a day's quantity. This single number decides Stage 5; it is worth more than three more quiet
nights on already-verified dates. ⚠ Remember the verdict line is untrustworthy (cumulative shadow).

**4. `toTargets.inputs.invoiceDataThrough`** — will read `2026-07-30` until Stage 5 flips. If it reads
`2026-07-31`, someone flipped Stage 5.

---

## 1. What is now automatic (the point of today)

```
21:55–23:55 IST  catalogue-sync-*      → skuMaster + priceData
00:35–04:00 IST  invoices-sync-window  → SHADOW row only  ← the one gap
04:35 + 05:25    sku-floors-sync       → newSKUQty            NEW today
05:45 + 06:15    engine-run-nightly    → params/toTargets     NEW today
~06:00 IST       ops POs start
hourly           stock ×4, orders
```

**Stage 5 is the only remaining gap.** Dead Stock and Min Req Qty stay manual **by design** (ops
judgement, not Zoho data) — they are not automation debt.

Flipping it: change `TARGET_ROW` in `sync-invoices` + redeploy, **plus a fresh dated backup**
(`invoice_data_backup_20260728` predates the 07-30 and 07-31 re-uploads; a stale backup of an
irreplaceable input is worse than none). Nothing before 2026-07-01 is re-fetchable.

---

## 2. Today, in order

1. **Morning checks passed.** Catalogue gate proven — 5 slots, 5 skips, 0 Zoho calls. Invoice night 2
   published clean; the D-3 re-fetch healed 07-27 from 0.6% → 0.1%.
2. **A ~50-min org-wide Zoho 429 window** (15:41–16:30 UTC on 07-30) was root-caused and recorded in
   `CLAUDE.md`. Second occurrence in two days; a third makes it a pattern. Instrumenting
   `zohoClient` was **considered and rejected** — shared path for six functions, hazard on the
   `retry429:false` branch only `create-to` uses, diagnostic-only payoff.
3. **`sync-sku-floors` shipped + scheduled** (Stage 8 in `CLAUDE.md`).
4. **Stage 6 shipped + scheduled LIVE** — `api/run-engine.js` on Vercel, pg_cron-driven.
5. **Invoice data refreshed to 07-30** by CSV (May02→Jul30; dropped Apr 30 + May 1, outside the 45d
   window — those two days now live **only** in `~/Downloads/Invoices - Apr30_Jul28.csv`, keep it).
6. **DS Seed SUNSET** — `dsSeed = {}`. Sandy's argument, confirmed by measurement. Full account in
   `CLAUDE.md`.
7. **DS06 floors added** — sheet 260 → 913. Restores deliberate floor intent without re-creating the
   seed: **+164 SKUs / +₹9.4L**, vs the seed's +431/+₹36.7L.
8. **Tabs attribution fix + DS06 blind spots** (see `CLAUDE.md`, Demand Attribution).
9. **`toTargets` one-shape fix** — both writers now stamp `inputs` + `engineCommit`.

**git:** `main` == `origin/main`, clean. Today: `26cd471` `b9344aa` `62deb42` `e74d854` `1b17ce9`
`541ec68` `9524eb9` `c27399c` `623c145`.
**Frontend:** `index-D0PPBUMS.js`, carrying SHA `c27399c737ef` inline. ⚠ A Vercel build's hash will
**not** match a local `npm run build` — `__ENGINE_COMMIT__` differs (`"local"`). Expected.
**Tests: 397 / 33 files.** Lint 68 errors (baseline).

**Live data state:**

| Row | State |
|---|---|
| `team_data/global` | skuMaster **2,105** (2,101 active) · priceData **1,858** · newSKUQty **1,115** (DS06 floors **913**) · minReqQty 1,921 · deadStock 20 |
| `team_data/invoice_data` | **75,699 rows · 2026-05-02 → 07-30** |
| `team_data/invoice_data_shadow` | 5,307 rows, 07-26 → 07-30 (Stage 4) |
| `params/toTargets` | 2,030 SKUs · written by the **browser** at 10:33Z, so **no `inputs`/`engineCommit` yet** — fills on the 05:45 run |
| `params/global` | `dsSeed={}` · attribution `shippingCode`, 128 pincodes (DS06 = 16) |
| backups | `sku_floors_backup_20260731` · `catalogue_backup_20260729` · `invoice_data_backup_20260728`/`_20260729` |

---

## 3. Commands

```bash
npx vite-node scripts/diff-headless-totargets.mjs   # headless vs browser Apply — the drift detector
npx vite-node scripts/dryrun-sku-floors.mjs         # sheet vs prod floors, read-only
node scripts/compare-invoice-shadow.mjs             # Stage 5 gate (⚠ cumulative verdict line)

# engine by hand (mode defaults to "dry" — writes nothing)
curl -sS -X POST https://homerun-ims.vercel.app/api/run-engine \
  -H "Content-Type: application/json" -H "x-engine-secret: <ENGINE_RUN_SECRET>" -d '{"mode":"dry"}'

select jobname, schedule from cron.job order by jobname;   # expect 10
```

"Did the cron fire?" without any code change: `function_edge_logs` + `execution_time_ms` — see
`CLAUDE.md`. ⚠ Cap log queries or a truncated result reads as "zero events".

---

## 4. Still open

1. **Stage 5** — the only automation gap. Gated on check #3. Take a fresh backup first.
2. **TO tool: "Min/Max computed at …"** — brief at `docs/TO-TOOL-MINMAX-FRESHNESS.md`, to be done in
   `homerun-to`. ⚠ That repo has **one unpushed commit** (`d051dd0`, docs-only) — push before starting.
   ⚠ Do **not** reuse `isFresh()`'s 15-min default; Min/Max refreshes daily, so use a day-based rule.
3. **IMS freshness pill** — `toTargets.refreshedAt` / `invoiceDataThrough` / `engineCommit` are all
   stamped and displayed nowhere. Frontend-only, lowest-risk item left.
4. **The stale-tab compute gap** — a long-lived tab computes from a stale catalogue. Naively adding
   `skuMaster` to the realtime handler causes ~5 engine re-runs an hour; reasoning is at the handler.
5. **125 active SKUs in categories with no strategy** — Home Appliances 57, Glass Hardware 31,
   Service 2. Premium slow-movers on Standard averaging. Its own diff.
6. **Optional:** Excess Qty (`SoH − Max`) on the Stock Health CSV for reverse TOs.

---

## 5. Blocked on you / ops

- **Set `cf_inventorised_at` in Zoho** for the SKUs in `catalogueSyncStatus.master.newSkusDefaulted`
  (12 on 07-30). They are active, defaulted to DC, and mostly unpriced — which makes PCT use the 95th
  percentile, the most aggressive setting. Re-derive from that row, not from an old list.
- **Confirm ops routing follows the pincode mapping** — DS02 is stocked ₹16.4L lighter.
- **Watch DS02 and DS05** in Stock Health.
- **Flag the DC team:** today's numbers moved twice deliberately (invoice refresh + DS06 floors), both
  upward for DS06.

---

## 6. Rollback

| If | Then |
|---|---|
| Engine auto-publish misbehaves | `select cron.unschedule('engine-run-nightly');` — then click Apply in IMS; `toTargets` is fully reconstructable in seconds. That asymmetry is why it shipped fast |
| Floors sync misbehaves | `select cron.unschedule('sku-floors-sync');` — CSV upload still works |
| A bad floors write | Restore `team_data/sku_floors_backup_20260731` → `newSKUQty` (read-merge-write) |
| Frontend regression | Vercel → previous deployment → **Promote** (instant), or revert the commit |
| DS Seed removal was wrong | Re-tick in Logic Tweaker + Apply. ⚠ Re-measure first — see `CLAUDE.md`; do not rollback on the old note |
| Catalogue / invoice sync | `select cron.unschedule('catalogue-sync-earlier' / 'invoices-sync-window');` |
